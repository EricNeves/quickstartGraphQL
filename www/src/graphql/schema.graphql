type Query {
  getUser(id: String): User
}
type User {
  id:    String
  name:  String
  email: String
}
type Mutation {
  updateUser(input: bodyUser): User
}
input bodyUser {
  name: String
  email: String
}